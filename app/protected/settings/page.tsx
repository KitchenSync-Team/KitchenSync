export default function SettingsPage() {
  return (
    <section className="flex flex-col gap-6 py-6">
      <header className="space-y-2">
        <h1 className="text-2xl font-semibold tracking-tight">Settings</h1>
        <p className="text-sm text-muted-foreground">
          Replace this placeholder with KitchenSync preferences once we wire real data.
        </p>
      </header>
      <div className="rounded-xl border border-dashed bg-muted/30 p-6 text-sm text-muted-foreground">
        This is placeholder content rendered inside the shared dashboard shell.
      </div>
    </section>
  );
}
